global:
  scrape_interval: ${PROMETHEUS_SCRAPE_INTERVAL:-15s}

scrape_configs:
  - job_name: 'translation-api'
    static_configs:
      - targets: ['nginx:80']  # scrape nginx which proxies to the app
    metrics_path: '/metrics'